server:
  port: 8080
  servlet:
    context-path: /identity



spring:
  application:
    name: identityservice
  datasource:
    url: "jdbc:mysql://localhost:3306/identity_service"
    username: "root"
    password: "Do200102!"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
